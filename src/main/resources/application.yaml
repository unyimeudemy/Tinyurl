spring:
  data:
    mongodb:
      uri: ${DB_URL}
      database: Tinyurl

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG